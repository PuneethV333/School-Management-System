import { useFetchMe } from "../../../hooks/useAuth"

export const Ut = () => {
    const {data:userData,isPending:loading} = useFetchMe()
    // const {} 
   return (
    <div>
        
    </div>
  )
}
